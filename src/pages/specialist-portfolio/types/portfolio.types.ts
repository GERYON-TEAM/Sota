export type Skill = {
  name: string
  progress: number
}

export type SkillType = 'hard' | 'soft'

export type Goal = {
  title: string
  timeline: string
  progress: string
  description: string
  status: string
}
