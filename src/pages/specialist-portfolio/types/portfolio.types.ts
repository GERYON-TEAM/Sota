export type Skill = {
  name: string
  level: string
  progress: number
}

export type SkillType = 'hard' | 'soft'

export type Goal = {
  title: string
  timeline: string
  progress: string
  description: string
  status: string
}
